services:
  - name: web
    type: web_service
    env: python
    buildCommand: python3 -m pip install -r requirements.txt
    startCommand: gunicorn main:app -b :$PORT --timeout 900 --workers 50 --threads 4
    plan: team
    healthCheckPath: /
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
